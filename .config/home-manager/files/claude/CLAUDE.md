# Global Instructions

## Git commits

Do not add a `Co-Authored-By` trailer to git commit messages.

## Memory

When the mnemon tool is available, always use mnemon for memory, not the file-based `.md` memory system. Do not create or update `.md` memory files when mnemon is active.
