#!/usr/bin/env bash
# Launch bottom (btm) with CPU-focused layout
# This script is called when clicking the CPU module in waybar

# Use custom config with CPU-focused layout
exec btm --config ~/.config/bottom/bottom-cpu.toml
