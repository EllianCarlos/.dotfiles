#!/usr/bin/env bash
# Launch bottom (btm) with memory-focused layout
# This script is called when clicking the memory module in waybar

# Use custom config with memory-focused layout
exec btm --config ~/.config/bottom/bottom-memory.toml
